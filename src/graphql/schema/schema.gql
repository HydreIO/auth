directive @Auth(canAccessTokenBeExpired: Boolean) on FIELD_DEFINITION

type Stage {
	stage: String!
	version: String!
}

type Query {
	# pong
	ping: String!
	# stage and version
	stage: Stage!
	# public key
	cert: String!
}