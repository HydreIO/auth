{
 "name": "@hydre/auth",
 "version": "1.0.11",
 "scripts": {
  "build:mongo": "parcel build packages/auth-server-mongo --no-source-maps --no-autoinstall",
  "start:koa": "node --harmony --experimental-specifier-resolution=node -r dotenv/config packages/auth-koa/src/index.js",
  "postversion": "git push && git push --tags"
 },
 "devDependencies": {
  "dotenv": "^8.2.0",
  "parcel": "^2.0.0-alpha.3.2"
 }
}
